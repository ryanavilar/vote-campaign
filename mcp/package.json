{
  "name": "vote-campaign-mcp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "MCP server exposing admin-scope read/write access to the Ikastara Kita campaign database (no delete operations).",
  "main": "server.mjs",
  "scripts": {
    "start": "node --env-file=.env.local server.mjs",
    "dev": "node --env-file=.env.local --watch server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@supabase/supabase-js": "^2.45.0"
  }
}
