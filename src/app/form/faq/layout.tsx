import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "FAQ Pendaftaran DPT — Munas XI Ikastara 2026",
  description: "Panduan lengkap & troubleshooting pendaftaran DPT Munas XI Ikastara 2026 — dari isi GForm sampai verifikasi web DPT.",
};

export default function FaqLayout({ children }: { children: React.ReactNode }) {
  return children;
}
